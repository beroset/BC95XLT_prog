set(EXECUTABLE_NAME "BC95XLT_prog")
find_package(Asio REQUIRED)
add_executable(${EXECUTABLE_NAME} main.cpp Channel.cpp Radio.cpp)
target_compile_definitions(${EXECUTABLE_NAME} PRIVATE -DASIO_STANDALONE)
target_include_directories(${EXECUTABLE_NAME} PRIVATE ${ASIO_INCLUDE_DIR})
install(TARGETS ${EXECUTABLE_NAME} DESTINATION bin)
